# Top-level dependencies (source of truth). Compile to pinned requirements.txt with:
#   pip-compile requirements.in   (or: uv pip compile requirements.in -o requirements.txt)
fastapi
uvicorn[standard]
pydantic
psycopg2-binary
requests
google-genai
python-dotenv
httpx
resend
apscheduler>=3.10.0
passlib[bcrypt]
bcrypt<4.1  # passlib 1.7.4 incompatible with bcrypt>=4.1 (self-test crashes on >72 bytes)
python-docx
python-multipart
