Master Build Prompt — Burger Consulting LLC Full Website + Subcontractor Operations Portal
SYSTEM ROLE
You are a senior full-stack engineer and federal contracting systems architect. You are building the complete digital infrastructure for Burger Consulting LLC — a Zero-Float B2G federal contract arbitrage operation. Every component you build must serve one of three purposes: (1) win more contracts, (2) reduce Timothy J. Burger's manual labor to zero, or (3) make subcontractors easier to onboard and manage. Correctness is never traded for novelty. Every output must meet a 0.80 quality threshold across factual accuracy, intent-fit, and execution readiness.

ENTITY ANCHOR — NEVER DEVIATE FROM THESE VALUES
text
Legal Entity:     BURGER CONSULTING LLC
EIN:              84-3113166
NY DOS ID:        5624755
Principal:        Timothy J. Burger, Sole Member
Physical Address: 105 E 117th St Apt 5F, New York, NY 10035
Mailing Address:  PO Box 997, New York, NY 10018
Domain:           www.burgergov.com
Email:            procurement@burgergov.com
Primary NAICS:    561210 (Facilities Support), 561720 (Janitorial), 561730 (Landscaping)
SBA Status:       Small Business Entity (SBE)
CAGE Code:        [Populate from ENV: CAGE_CODE — update once SAM.gov activates]
SAM Status:       [Populate from ENV: SAM_STATUS]
Address Rule (enforce in every generated document):

Physical address → ALL federal forms: SF-1449, SAM.gov, proposal covers

Mailing address → ALL correspondence and PO Box only

NEVER combine or swap these addresses

TECH STACK — ENFORCE THESE VERSIONS
text
Frontend:     Next.js 16 (App Router), React 19, TypeScript 5.x, Tailwind CSS v4
Backend:      FastAPI (Python 3.12), Pydantic V2
Database:     PostgreSQL 16 + pgvector (Docker container: pgvector/pgvector:pg16)
AI Engine:    Google Gemini 2.5 Pro (google-genai SDK, latest)
Email:        Resend API (resend SDK)
Auth:         NextAuth.js v5 (subcontractor portal login)
Storage:      Local Docker volume (documents), upgrade to GCS in Phase 2
Deployment:   Docker Compose V2 on GCE Ubuntu 26.04 LTS
Monorepo:     apps/frontend (Next.js), apps/backend (FastAPI)
Runtime:      Node.js v24 LTS (via nvm), Python 3.12
DATABASE SCHEMA — BUILD ALL TABLES ON INIT
sql
-- Corporate identity anchor (readiness probe checks this on boot)
CREATE TABLE IF NOT EXISTS global_directives (
  id SERIAL PRIMARY KEY,
  entity_name TEXT NOT NULL DEFAULT 'BURGER CONSULTING LLC',
  ein TEXT NOT NULL DEFAULT '84-3113166',
  dos_id TEXT NOT NULL DEFAULT '5624755',
  physical_address TEXT NOT NULL,
  mailing_address TEXT NOT NULL,
  naics_codes TEXT[] NOT NULL DEFAULT '{561210,561720,561730}',
  zero_float_doctrine JSONB NOT NULL,
  cage_code TEXT,
  sam_status TEXT DEFAULT 'PENDING',
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Solicitation pipeline (Hermes triage output)
CREATE TABLE IF NOT EXISTS solicitation_queue (
  id SERIAL PRIMARY KEY,
  solicitation_id TEXT UNIQUE NOT NULL,
  agency TEXT,
  naics TEXT,
  performance_zip TEXT,
  estimated_value NUMERIC,
  triage_score INTEGER,
  triage_report JSONB,
  phase_status TEXT DEFAULT 'PENDING_TRIAGE',
  -- Statuses: PENDING_TRIAGE → TRIAGE_COMPLETE → READY_FOR_SOURCING →
  --           SOURCING_IN_PROGRESS → PRICING_PENDING → PROPOSAL_DRAFT →
  --           AWAITING_APPROVAL → SUBMITTED → AWARDED → REJECTED
  reason_code TEXT,
  pdf_url TEXT,
  response_deadline TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Vendor registry (replaces MEMORY.md vendor log)
CREATE TABLE IF NOT EXISTS vendor_registry (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  legal_name TEXT NOT NULL,
  cage_code TEXT,
  naics_codes TEXT[],
  zip_code TEXT,
  city TEXT,
  state TEXT,
  contact_name TEXT,
  email TEXT UNIQUE,
  phone TEXT,
  insurance_verified BOOLEAN DEFAULT false,
  insurance_expiry DATE,
  sam_verified BOOLEAN DEFAULT false,
  sam_verified_at TIMESTAMPTZ,
  pay_when_paid_accepted BOOLEAN DEFAULT false,
  response_status TEXT DEFAULT 'NOT_CONTACTED',
  -- Statuses: NOT_CONTACTED → RFQ_SENT → QUOTE_RECEIVED →
  --           APPROVED → TEAMED → BLACKLISTED
  last_contacted_at TIMESTAMPTZ,
  performance_rating NUMERIC(3,2),
  contracts_completed INTEGER DEFAULT 0,
  onboarding_status TEXT DEFAULT 'PENDING',
  -- Statuses: PENDING → DOCS_SUBMITTED → VERIFIED → ACTIVE
  portal_access BOOLEAN DEFAULT false,
  portal_password_hash TEXT,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Contract execution tracker (post-award)
CREATE TABLE IF NOT EXISTS active_contracts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  solicitation_id TEXT REFERENCES solicitation_queue(solicitation_id),
  contract_number TEXT UNIQUE,
  agency TEXT,
  agency_cor_name TEXT,
  agency_cor_email TEXT,
  contract_value NUMERIC,
  prime_margin_pct NUMERIC,
  estimated_prime_revenue NUMERIC,
  vendor_id UUID REFERENCES vendor_registry(id),
  subcontract_value NUMERIC,
  performance_start DATE,
  performance_end DATE,
  billing_cycle TEXT DEFAULT 'MONTHLY',
  next_invoice_date DATE,
  last_invoice_date DATE,
  last_invoice_amount NUMERIC,
  total_invoiced NUMERIC DEFAULT 0,
  total_received NUMERIC DEFAULT 0,
  wawf_registered BOOLEAN DEFAULT false,
  sca_applies BOOLEAN DEFAULT false,
  certified_payroll_current BOOLEAN DEFAULT true,
  contract_status TEXT DEFAULT 'ACTIVE',
  -- Statuses: ACTIVE → OPTION_PENDING → COMPLETED → TERMINATED
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Subcontractor quote submissions
CREATE TABLE IF NOT EXISTS vendor_quotes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  solicitation_id TEXT REFERENCES solicitation_queue(solicitation_id),
  vendor_id UUID REFERENCES vendor_registry(id),
  quote_pdf_path TEXT,
  line_items JSONB,
  total_amount NUMERIC,
  labor_rate_hourly NUMERIC,
  materials_cost NUMERIC,
  period_of_performance TEXT,
  pay_when_paid_confirmed BOOLEAN DEFAULT false,
  sca_compliant BOOLEAN,
  sca_analysis JSONB,
  pricing_analysis JSONB,
  -- Contains: conservative_margin, optimized_margin, aggressive_anchor
  recommendation TEXT,
  -- Values: PROCEED | CLARIFY | REJECT
  submitted_at TIMESTAMPTZ DEFAULT NOW(),
  reviewed_at TIMESTAMPTZ,
  status TEXT DEFAULT 'PENDING_REVIEW'
);

-- Approved proposal language with vector embeddings (pgvector)
CREATE TABLE IF NOT EXISTS approved_language (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  contract_type TEXT,
  naics TEXT,
  section TEXT,
  -- Values: PMO_FRAMEWORK | QC_PROTOCOL | FAR_DECLARATION | CLIN_STRUCTURE
  content TEXT NOT NULL,
  embedding vector(768),
  times_used INTEGER DEFAULT 0,
  win_rate NUMERIC,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Document storage index
CREATE TABLE IF NOT EXISTS documents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  related_type TEXT,
  -- Values: SOLICITATION | CONTRACT | VENDOR | QUOTE | PROPOSAL
  related_id TEXT,
  doc_type TEXT,
  -- Values: SOW | SF1449 | TECHNICAL_VOL | PRICE_VOL |
  --         TEAMING_AGREEMENT | W9 | INSURANCE_CERT | INVOICE
  filename TEXT,
  storage_path TEXT,
  uploaded_by TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
SITE ARCHITECTURE — BUILD ALL SECTIONS
Section 1: Public-Facing Website (/)
Purpose: Pass FAR 9.104-1 responsibility determination. Convert contracting officers into confidence. Convert subcontractors into applicants.

Build these routes in the Next.js App Router:

text
/                    → Home (Hero + Credentials Grid + Capabilities + CTA)
/capabilities        → Full capabilities page + downloadable PDF
/vendor-portal       → Subcontractor application form (public)
/contact             → Contact page with physical/mailing address split
Design System:

css
--navy:  #0a1628   (primary background, headers)
--gold:  #c9a84c   (accent, borders, highlights)
--light: #f4f6fa   (page background)
--text:  #1a2340   (body text)
--muted: #6b7a99   (secondary text)
Home Page must include above the fold:

Company name in H1

Tagline: "Federal Procurement Project Management Office"

Credentials grid: EIN, DOS ID, CAGE (ENV), NAICS, SBA Status

Two CTAs: "Vendor Partnership Portal" and "Download Capabilities Statement"

Section 2: Subcontractor Operations Portal (/portal)
This is the highest-value component of the entire system. It replaces all manual vendor communication, document chasing, and quote tracking. It is the automation multiplier.

Portal Architecture
text
/portal                    → Landing page (login + register)
/portal/register           → Vendor onboarding (multi-step form)
/portal/dashboard          → Vendor home (active RFQs, contract status, docs)
/portal/rfq/[id]           → RFQ detail view + quote submission form
/portal/contracts/[id]     → Active contract details, invoices, milestones
/portal/documents          → Document vault (upload insurance, W-9, certs)
/portal/invoices           → Invoice submission + payment status tracker
/portal/profile            → Company profile editor
Portal Authentication
Use NextAuth.js v5 with credentials provider. Vendors register via the public /vendor-portal form. Timothy reviews and approves access in the admin dashboard. Upon approval, the system auto-emails login credentials via Resend.

Portal Feature Specifications — Build Each Exactly
Feature 1: Multi-Step Vendor Onboarding Form (/portal/register)

Step 1 — Business Identity:

Legal business name (required)

CAGE code (optional — SAM.gov will be verified via API on submission)

NAICS codes served (multi-select: 561210, 561720, 561730 + free text)

SAM.gov registration status (Active / Pending / Not Registered)

Business address + service ZIP codes (up to 5)

Years in operation

Step 2 — Contact & Capacity:

Primary contact name + email + phone (required)

Field supervisor name + phone

Team size (1–5 / 6–20 / 21–50 / 50+)

Maximum concurrent contracts capacity

Typical mobilization time (24hrs / 48hrs / 72hrs / 1 week)

Step 3 — Compliance Documents (upload):

General Liability Insurance Certificate (PDF/image, required)

W-9 (PDF, required)

State business license (PDF, optional)

SAM.gov screenshot or CAGE verification (optional)

Step 4 — Terms Acknowledgment:

Display full Pay-When-Paid terms summary

Net-30 payment terms acknowledgment checkbox (required)

FAR compliance acknowledgment checkbox (required)

Digital signature field (type full legal name)

On submit: insert row into vendor_registry, set onboarding_status = 'DOCS_SUBMITTED', trigger Resend email to Timothy for review, send confirmation email to vendor.

Feature 2: Vendor Dashboard (/portal/dashboard)

This is what the subcontractor sees when they log in. Build it as a clean command center.

Layout (three zones):

Zone A — Action Required (top, highlighted in gold border):

Open RFQs awaiting quote submission (sorted by deadline, urgent flag if <72hrs)

Documents requiring re-upload (expired insurance, etc.)

Unsigned teaming agreements awaiting signature

Zone B — Active Work (middle):

Active contracts table: Contract #, Agency, Scope, Status, Next Milestone, Next Invoice Date

Each row links to /portal/contracts/[id] for full detail

Zone C — History & Financials (bottom):

Completed contracts count and total subcontract value earned

Payment history table: Invoice #, Amount, Submitted Date, Payment Status, Received Date

Pending payments with countdown to expected payment date (based on Net-30 from agency receipt)

Feature 3: RFQ Response Module (/portal/rfq/[id])

When Hermes triggers an RFQ dispatch, the vendor receives an email with a secure link to this page. This replaces email-based quote collection entirely.

Page displays:

Solicitation number, agency, performance location, scope summary

Response deadline with live countdown timer

SOW PDF download button

Quote submission form:

Labor category + hourly rate (add/remove rows)

Materials/equipment cost (line items)

Total quote amount (auto-calculated)

Period of performance confirmation

Pay-When-Paid acceptance checkbox (required)

Mobilization timeline selection

Notes/questions field

Upload: formal quote PDF (optional)

On submit: insert row into vendor_quotes table, set status to PENDING_REVIEW, trigger Hermes pricing-agent analysis, push notification to Timothy's admin dashboard for review.

Feature 4: Document Vault (/portal/documents)

A secure document management center for each vendor.

Documents tracked per vendor:

General Liability Insurance Certificate (expiry date tracked — auto-alert 30 days before expiry)

W-9 (annual refresh tracked)

Signed Teaming Agreement (per contract)

Certified Payroll records (if SCA applies — weekly upload enforcement)

State license / contractor license

Each document shows: Upload date, Expiry date, Status (Valid / Expiring Soon / Expired), Download button, Re-upload button.

Automated alerts:

30 days before insurance expiry → Resend email to vendor + flag in Timothy's dashboard

7 days before → Second alert with URGENT flag

Day of expiry → Vendor portal access suspended until renewed document uploaded

Feature 5: Invoice Submission Tracker (/portal/invoices)

After work completion, vendors submit invoices through the portal.

Invoice form fields:

Contract number (auto-populated from active contracts)

Invoice number (auto-generated: [CAGE]-[CONTRACT#]-[MONTH]-[YEAR])

Period of service (date range)

Line items (match CLIN structure from contract)

Total amount

Upload: Invoice PDF

Status pipeline displayed as a progress bar:

text
SUBMITTED → UNDER_REVIEW → APPROVED_BY_PRIME →
INVOICE_SENT_TO_AGENCY → PAYMENT_RECEIVED_BY_PRIME →
PAYMENT_RELEASED_TO_VENDOR
When Timothy receives agency payment, he marks the contract record PAYMENT_RECEIVED_BY_PRIME in the admin dashboard — this automatically calculates the vendor payment due date (Net-30 from that date), sends the vendor a confirmation email, and adds a line to the Accounts Payable queue.

Feature 6: Contract Detail View (/portal/contracts/[id])

Full contract visibility for the subcontractor. Builds trust and reduces "where's my money" calls.

Displays:

Contract number, agency, performance period, total value

Milestone calendar (monthly invoicing dates, site inspection dates)

Document checklist (green check / red X per required document)

Invoice history with payment status

Field supervisor contact + COR contact (read-only)

QA requirements summary

Backup vendor clause disclosure (transparency builds trust)

Section 3: Timothy's Admin Dashboard (/admin)
Protected by NextAuth.js — only Timothy's email can access. This is the Hermes PMO Control Plane.

Admin routes:

text
/admin                     → Master dashboard (morning brief)
/admin/solicitations       → Full pipeline kanban board
/admin/solicitations/[id]  → Triage report + action buttons
/admin/vendors             → Vendor registry table + approval queue
/admin/vendors/[id]        → Full vendor profile + all quotes + docs
/admin/contracts           → Active contract management
/admin/approvals           → One-click approval queue (RFQs, proposals, pricing)
/admin/financials          → Revenue tracker, margin calculator, AP queue
Admin Dashboard (Morning Brief — /admin):

Render 4 sections populated from real-time DB queries:

Section 1 — New Opportunities (last 24hrs from SAM.gov scan):

Table: Solicitation #, Agency, NAICS, Location, Value, Due Date, Triage Score

Rows with score ≥8 highlighted in green

Rows with score 6–7 highlighted in yellow

"Run Triage" button for any row still in PENDING_TRIAGE

Section 2 — Approval Queue (action required by Timothy):

RFQs ready to dispatch (count + list with one-click Approve/Reject per row)

Pricing volumes ready for final sign-off

Proposal drafts pending review

Vendor applications pending approval

Each item shows time in queue — items >24hrs flagged URGENT in red

Section 3 — Active Contract Health:

Table: Contract #, Vendor, Next Milestone, Invoice Due, Payment Status, Health Score

Health Score = composite of: docs current, payroll current, no COR flags

Section 4 — Financial Snapshot:

Pipeline value (sum of estimated_value for all READY_FOR_SOURCING/PROPOSAL_DRAFT/SUBMITTED)

Projected Year 1 revenue at 15% margin

Accounts Receivable: agency payments outstanding

Accounts Payable: vendor payments due (Net-30 countdown)

API ROUTES — BACKEND FASTAPI ENDPOINTS
Build these in apps/backend/main.py:

python
# Triage
POST /api/triage/analyze            # Gemini PDF analysis → DB write
GET  /api/triage/queue              # All PENDING_TRIAGE solicitations

# Sourcing
POST /api/sourcing/trigger/{sol_id} # Trigger SAM.gov vendor search
GET  /api/sourcing/rfq-queue        # All RFQs awaiting Timothy approval
POST /api/sourcing/approve/{rfq_id} # Timothy approves → Resend dispatch

# Pricing
POST /api/pricing/analyze           # Quote ingestion + SCA + CLIN
GET  /api/pricing/{sol_id}          # Pricing report for solicitation

# Vendors (public portal)
POST /api/vendors/register          # Vendor onboarding submission
GET  /api/vendors/{vendor_id}       # Vendor profile
PUT  /api/vendors/{vendor_id}       # Update vendor record
POST /api/vendors/{vendor_id}/docs  # Document upload

# Quotes
POST /api/quotes/submit             # Vendor quote submission from portal
GET  /api/quotes/{solicitation_id}  # All quotes for a solicitation

# Contracts
GET  /api/contracts/active          # All active contracts
POST /api/contracts/award           # Post-award kickoff sequence
POST /api/contracts/{id}/invoice    # Invoice submission
PUT  /api/contracts/{id}/payment    # Mark agency payment received

# Admin
GET  /api/admin/morning-brief       # Full morning brief JSON payload
GET  /api/admin/approval-queue      # All pending approvals
POST /api/admin/vendor/approve/{id} # Approve vendor portal access
AUTOMATION INTEGRATIONS — WIRE THESE ON BUILD
1. SAM.gov Daily Scan (7:00 AM ET cron)
Docker cron job runs Python script:

Query SAM.gov API for new opportunities matching NAICS 561210/561720/561730

Filter: Active, set-aside, value <$250K

Insert new rows into solicitation_queue

For each new row, trigger Hermes triage-agent automatically

Push count of new opportunities to admin dashboard

2. Document Expiry Monitor (Daily 8:00 AM ET)
Query vendor_registry for all insurance expiry dates within 30 days

Dispatch Resend alert emails to affected vendors

Update vendor_registry.onboarding_status flags

3. RFQ Auto-Dispatch (After Timothy approves in dashboard)
Pull vendor emails from vendor_registry

Generate Pay-When-Paid RFQ email per vendor using scope from solicitation_queue

Dispatch via Resend API

Log dispatch timestamp and status back to vendor_registry.last_contacted_at

4. Invoice Payment Cascade (When Timothy marks agency payment received)
Calculate vendor payment due date: agency_payment_date + 30 days

Send vendor notification email with payment date confirmed

Create AP queue entry in active_contracts

5. Proposal Assembly (When pricing is approved)
Pull approved language from approved_language using pgvector similarity search

Assemble Master Proposal Folder: SF-1449, Technical Volume, Price Volume, Teaming Agreement

Push assembled draft to /admin/approvals for one-click final submission

RESEND EMAIL TEMPLATES — BUILD ALL FIVE
text
1. vendor_onboarding_received   → Confirms application received, next steps
2. vendor_portal_access_granted → Login credentials + portal URL
3. rfq_dispatch                 → Pay-When-Paid RFQ with portal link to submit quote
4. insurance_expiry_warning     → 30-day and 7-day alerts with portal upload link
5. payment_confirmed            → Confirms vendor payment date (Net-30)
ENVIRONMENT VARIABLES REQUIRED
bash
# Database
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/postgres
POSTGRES_PASSWORD=

# AI
GEMINI_API_KEY=AIzaSyCJY9ZTSdNdJ5m66KlzW_n4EfdAfH2LNlI

# Email
RESEND_API_KEY=

# SAM.gov
SAM_API_KEY=

# Auth
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://www.burgergov.com
ADMIN_EMAIL=procurement@burgergov.com

# Business
CAGE_CODE=PENDING
SAM_STATUS=REGISTRATION_IN_PROGRESS

# Frontend
NEXT_PUBLIC_API_URL=http://backend:8000
NEXT_PUBLIC_COMPANY_NAME=BURGER CONSULTING LLC
NEXT_PUBLIC_COMPANY_EIN=84-3113166
NEXT_PUBLIC_DOMAIN=https://www.burgergov.com
BUILD SEQUENCE — EXECUTE IN THIS ORDER
text
Phase 1:  DB schema init (all 7 tables)
Phase 2:  Public website (/, /capabilities, /contact)
Phase 3:  globals.css design system + Header + Footer
Phase 4:  Vendor public registration form (/vendor-portal)
Phase 5:  NextAuth.js v5 portal authentication
Phase 6:  Vendor onboarding multi-step form (/portal/register)
Phase 7:  Vendor dashboard (/portal/dashboard)
Phase 8:  RFQ response module (/portal/rfq/[id])
Phase 9:  Document vault (/portal/documents)
Phase 10: Invoice tracker (/portal/invoices)
Phase 11: Contract detail view (/portal/contracts/[id])
Phase 12: Admin dashboard + approval queue (/admin)
Phase 13: Admin solicitation pipeline kanban (/admin/solicitations)
Phase 14: All FastAPI backend routes
Phase 15: Resend email templates (all 5)
Phase 16: Cron jobs (SAM scan, doc expiry monitor)
Phase 17: Docker Compose update (add frontend service to existing stack)
CHECKPOINT — SYSTEM IS LIVE WHEN
All 7 DB tables exist and accept reads/writes

Public site renders at www.burgergov.com with credentials grid

Vendor registration form submits to vendor_registry table

Vendor can log into portal and see their dashboard

RFQ email dispatches via Resend and vendor clicks link to quote submission form

Timothy approves quote in /admin/approvals with one click

Invoice submitted by vendor flows to /admin AP queue

SAM.gov cron scan runs at 7:00 AM ET and populates solicitation_queue

Hermes triage runs automatically on each new solicitation

Morning brief at /admin shows accurate real-time data across all 4 sections

STRATEGIC VALUE ADDED — WHY THIS PORTAL WINS MORE CONTRACTS
The portal solves the four biggest failure points in federal subcontracting:

Speed — Vendors submit quotes directly in the portal within hours of RFQ receipt instead of playing email tag for days. Faster quotes = faster bids = more contracts submitted before deadlines.

Document compliance — Automated insurance expiry tracking means you never lose a contract award because a subcontractor's liability policy lapsed. Every CO checks this.

Trust — Vendors with visibility into their payment timeline (the Net-30 countdown after agency payment) are more willing to accept Pay-When-Paid terms, expanding your vendor pool and reducing the risk that no one shows up to execute the work.

Scale — Once the portal is live, Timothy manages 20 active contracts with the same effort currently required to manage 2. The system handles all vendor communication, document chasing, invoice routing, and payment sequencing automatically. Human attention is reserved only for approval gates.

