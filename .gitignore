# Environment secrets
.env
.env.local
.envchmod

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/

# Node / Next.js
node_modules/
.next/
npm-debug.log*

# Database data
pgdata/

# OS
.DS_Store

# SSL certificates and private keys — never commit
nginx/certbot/conf/
nginx/certbot/www/

# Shell and system dotfiles
.bash_history
.bash_logout
.bashrc
.bash_profile
.profile
.ssh/
.cache/
.config/
.local/
.npm/
.nvm/
.docker/
.gitconfig
.claude.json
.claude/
backups/
upgrade.log
watchdog.log
*.log
snap/

# External cloned reference repo, not part of this project
Anthropic-Cybersecurity-Skills/

# Local build-spec planning artifact — never commit (contained secrets)
Master Build Prompt*.txt

# Local .env backups (contain live secrets) — never commit
.env.bak*
